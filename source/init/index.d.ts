/**
 * ```
 * init :: as => as
 * ```
 * -----------------------------------------------------------------------------
 *
 * Returns all but the last element in an array.
 *
 */
export declare function init(as: any[]): any[];
