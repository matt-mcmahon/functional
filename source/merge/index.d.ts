/**
 * ```
 * merge :: a => b => c
 * ```
 * -----------------------------------------------------------------------------
 *
 * Performs a shallow merge of two objects.
 *
 */
export declare function merge<A, B, C>(a: A, b: B): C
