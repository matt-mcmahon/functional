/**
 * ```
 * isDefined :: a => boolean
 * ```
 * -----------------------------------------------------------------------------
 *
 * Returns true unless a is `NaN`, `undefined`, or `null`
 *
 */
export declare function isDefined(value: any): boolean
