/**
 * ```
 * take :: (n, as) => as
 * ```
 * -----------------------------------------------------------------------------
 *
 * Returns the first _n_ elements of the given list.
 *
 */
export declare function take<A>(n: Number, list: A[]): A[];
