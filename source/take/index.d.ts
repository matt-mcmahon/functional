/**
 * ```
 * take :: (n, ...as) => as
 * ```
 * -----------------------------------------------------------------------------
 *
 * Returns the first _n_ elements of the gathered list.
 *
 */
export declare function take(n: Number, ...list: any[]): any[]
