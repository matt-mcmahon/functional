/**
 * ```
 * identity :: a => a
 * ```
 * -----------------------------------------------------------------------------
 *
 * Returns it's argument, unmodified.
 *
 */
export declare function identity<A>(a: A): A;
