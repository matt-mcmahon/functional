/**
 * ```
 * T :: a => true
 * ```
 * -----------------------------------------------------------------------------
 *
 * A function that always returns `true`.
 *
 */
export declare function T(): true;
