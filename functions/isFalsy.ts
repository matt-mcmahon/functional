/**
 * ```haskell
 * isFalsy :: a => boolean
 * ```
 * -----------------------------------------------------------------------------
 *
 * Returns `true` if __a__ is a falsy value, false otherwise.
 */
export const isFalsy = (a: unknown): boolean => !a;
