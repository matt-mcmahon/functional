/**
 * ```
 * take :: (n, as) => as
 * ```
 * -----------------------------------------------------------------------------
 *
 * Returns the first _n_ elements of the given list.
 *
 */
export declare const take: (n: number) => <A>(as: A[]) => A[];
