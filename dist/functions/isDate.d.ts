/**
 * ```
 * isDate :: a => boolean
 * ```
 * -----------------------------------------------------------------------------
 *
 * Returns `true` if a is a Date object, `false` otherwise.
 *
 */
export declare const isDate: (a: unknown) => a is Date;
