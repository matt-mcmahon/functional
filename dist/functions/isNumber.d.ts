/**
 * ```
 * isNumber :: a => boolean
 * ```
 * -----------------------------------------------------------------------------
 *
 * Returns `true` if __a__ has type `number`.
 *
 */
export declare const isNumber: (a: unknown) => a is number;
